<?xml version="1.0" encoding="utf-8" ?>
<!--
SPDX-FileCopyrightText: 2026 Coop IT Easy SC

SPDX-License-Identifier: AGPL-3.0-or-later
-->
<odoo>
    <record id="hr_work_entry_type_view_search" model="ir.ui.view">
        <field name="name">hr.work.entry.type.filter</field>
        <field name="model">hr.work.entry.type</field>
        <field name="arch" type="xml">
            <search string="Search Work Entry Type">
                <field name="name" />
                <field name="code" />
                <field name="country_code" optional="hidden" />
                <field name="leave_type_ids" optional="hidden" />
            </search>
        </field>
    </record>

    <record id="hr_work_entry_type_view_list" model="ir.ui.view">
        <field name="name">hr.work.entry.type.list</field>
        <field name="model">hr.work.entry.type</field>
        <field name="arch" type="xml">
            <list multi_edit="1" sample="1">
                <field name="name" />
                <field name="code" />
                <field name="external_code" />
                <field name="color" widget="color_picker" />
                <field name="country_code" optional="hide" />
                <field name="leave_type_ids" optional="hide" />
            </list>
        </field>
    </record>

    <record id="hr_work_entry_type_action" model="ir.actions.act_window">
        <field name="name">Work Entry Type</field>
        <field name="res_model">hr.work.entry.type</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="hr_work_entry_type.hr_work_entry_type_view_list" />
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No data to display
            </p>
            <p>
                Try to add some records, or make sure that there is no active filter in the search bar.
            </p>
        </field>
    </record>
</odoo>
